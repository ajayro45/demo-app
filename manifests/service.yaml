apiVersion: v1
kind: Service
metadata:
  name: simple-web-service
  namespace: demo
spec:
  selector:
    app: simple-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort # Change to LoadBalancer if using a cloud provider
